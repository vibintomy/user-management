import 'package:flutter/material.dart';

class UserStatsMobile extends StatelessWidget {
  const UserStatsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
