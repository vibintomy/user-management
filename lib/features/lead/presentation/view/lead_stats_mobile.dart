import 'package:flutter/material.dart';

class LeadStatsMobile extends StatelessWidget {
  const LeadStatsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
