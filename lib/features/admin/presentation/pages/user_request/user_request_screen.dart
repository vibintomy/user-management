import 'package:flutter/material.dart';

class UserRequestScreen extends StatelessWidget {
  const UserRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("friend request section")
        ],
      ),
    );
  }
}
